import { StyleSheet } from 'react-native';
import { WIDTH_FACTOR } from '../dimensionConstants';
export const globalStyles = StyleSheet.create({
    title: {
        justifyContent: 'center',
        fontSize: 55 * WIDTH_FACTOR,
        fontFamily: 'primitive',
        color: '#a0936c',
    },
});
